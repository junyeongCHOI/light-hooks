import UseDebounceExample from "../components/Hooks/UseDebounceExample";

<Meta title="Example/useDebounce" component={UseDebounceExample} />

# useDebounce

`debounced` 된 `함수`를 반환하는 hook입니다.

useDebounce(func, wait, options)

wait = number (ms)

options = { leading? : boolean, trailing? : boolean, maxWait? : number }

returns debounced function

# Example

<UseDebounceExample />
